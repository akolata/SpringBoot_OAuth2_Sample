server:
  port: 8083
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/oauth_tests
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
